class BaseRequest {
  static const String baseUrl = 'http://192.168.242.157:9000';
  static const Duration timeout = Duration(seconds: 8);
  static const Duration receiveTimeout = Duration(seconds: 8);
}
